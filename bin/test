#!/usr/bin/env ruby

require "bundler/setup"
require "very_good"

require "pry"
require "pry-stack_explorer"

VeryGood.start!

state = VeryGood.state

newState = state.set(0, 0, VeryGood::Cell.new("x", color: VeryGood::COLORS[:blue]))

VeryGood.state = newState
VeryGood.update!
